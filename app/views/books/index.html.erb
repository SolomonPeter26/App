<h1 class="headings" align="center">Welcome <%=  %></h1>
<table class = "table" align="left">
  <tr>
    <th>Title</th>
    <th>Pages</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @books.each do |book| %>
  <tr class = "telements">
    <td class="rows"><%= book.title %></td>
    <td class="rows"><%= book.pages %></td>
    <td class="rows"><%= link_to 'Show', book %></td>
  </tr>
<% end %>
</table>

<br />

